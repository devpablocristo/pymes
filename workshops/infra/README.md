# Workshops Infra

Scaffold reservado para la infraestructura independiente de la vertical `workshops`.

La prioridad en esta iteracion fue dejar operativo:

- backend Go del vertical
- integraciones HTTP con `pymes-core`
- frontend unificado
- compose/CI/smoke local

Si mas adelante se desacopla el deploy de `workshops`, la referencia a seguir es `professionals/infra/`.
