name: Deploy

on:
  workflow_dispatch:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder deploy
        run: |
          echo "Build backend image and deploy Lambda here"
          echo "Sync frontend dist to S3 + CloudFront invalidation here"
