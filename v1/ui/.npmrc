# platform-browser 0.2.0 is additive; platform-authn 0.1.0 still declares
# the narrower ^0.1.0 peer range. Keep npm ci reproducible until authn
# publishes a widened peer dependency.
legacy-peer-deps=true
